server:
  port: 8001
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/expertsconseil/microservices/git-services-config.git

  boot:
    admin:
      client:
        url: http://localhost:8002

